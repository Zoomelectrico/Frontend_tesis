import React from "react";

const studentCenterFederation = props => (
  <div className="container">
    <h2 className="title">
      <form onSubmit={props.submitFunction}>
        <p>adas</p>
      </form>
    </h2>
  </div>
);

export default studentCenterFederation;
